llm:
  provider: huggingface
  detection_model: meta-llama/Meta-Llama-3-8B-Instruct
  extraction_model: meta-llama/Meta-Llama-3-8B-Instruct

  # provider: ollama
  # detection_model: llama3.2:3b
  # extraction_model: qwen2.5:7b

  # provider: openai
  # api_key: your-api-key
  # base_url: http://localhost:8000/v1  # for vLLM or other OpenAI-compatible servers
  # detection_model: gpt-4o-mini
  # extraction_model: gpt-4o-mini
